on drawTripMap(t){const el=$('#tripMap');if(!el||!window.L)return;const center=CITY_GEO[t.destination]||[-20,130];leafletMap=L.map(el,{zoomControl:true}).setView(center,CITY_GEO[t.destination]?11:2);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{attribution:'© OpenStreetMap'}).addTo(leafletMap);(t.pois||[]).forEach(p=>{const geo=CITY_GEO[p.area]||CITY_GEO[p.area?.split(',')[0]];if(geo)L.marker(geo).addTo(leafletMap).bindPopup(esc(p.name))})}
async function loadTripLive(t){const geo=CITY_GEO[t.destination]||CITY_GEO[getCities(t)[0]];const tz=(COUNTRY_META[tripCountryIds(t)[0]]||{}).tz||'UTC';const renderTime=()=>{const now=new Date();const local=new Intl.DateTimeFormat('ja-JP',{timeZone:tz,hour:'2-digit',minute:'2-digit'}).format(now);const jst=new Intl.DateTimeFormat('ja-JP',{timeZone:'Asia/Tokyo',hour:'2-digit',minute:'2-digit'}).format(now);const box=$('#timeBox');if(box)box.innerHTML=`${esc(tz)} ${local}<br><span class="muted">JST ${jst}</span>`};renderTime();setTimeout(renderTime,60000);if(!geo)return;try{const r=await fetch(`https://api.open-meteo.com/v1/forecast?latitude=${geo[0]}&longitude=${geo[1]}&current=temperature_2m,weather_code,wind_speed_10m&timezone=auto`);const d=await r.json();const w=d.current;const box=$('#weatherBox');if(box)box.innerHTML=`<b>${w.temperature_2m}°C</b>　風 ${w.wind_speed_10m} km/h<div class="muted">Open-Meteo live</div>`}catch{const box=$('#weatherBox');if(box)box.textContent='天気を取得できませんでした'}}
function renderToday(){const t=activeTrip();if(!t){view.innerHTML='<div class="empty">Tripを作成してください</div>';return}const today=new Date().toISOString().slice(0,10);let d=(t.days||[]).find(x=>x.date===today)||(t.days||[]).find(x=>x.date>=today)||(t.days||[])[0];const items=d?.items||[];const now=new Date();const nowM=now.getHours()*60+now.getMinutes();let next=items.find(i=>{const m=String(i.time||'').match(/(\d{1,2}):(\d{2})/);return m&&Number(m[1])*60+Number(m[2])>=nowM})||items[0];view.innerHTML=`<div class="eyebrow">TODAY MODE</div><div class="big">${esc(t.title)}</div><div class="muted">${d?fmtDate(d.date):'予定なし'} ${esc(d?.title||'')}</div>${next?`<div class="card card-pad today-next" style="margin-top:18px"><div class="eyebrow">NEXT</div><div class="big">${esc(next.time||'')} ${esc(next.title||'')}</div><div class="muted">${esc(next.memo||'')}</div><div class="row wrap" style="margin-top:14px"><button class="btn primary" id="routeNext">Google Maps</button><span class="pill">JST ${esc(next.homeTime||'--:--')}</span></div></div>`:''}<div class="section-title"><h2>この日の予定</h2></div><div class="timeline">${items.map(i=>`<div class="event"><div class="dot">${i===next?'→':'•'}</div><div class="event-box"><strong>${esc(i.time||'')} ${esc(i.title||'')}</strong><div class="event-meta">${esc(i.timezone||'')} ${i.homeTime?`· JST ${esc(i.homeTime)}`:''}</div></div></div>`).join('')||'<div class="empty">予定なし</div>'}</div>`;if(next)$('#routeNext').onclick=()=>openMaps(resolveItemQuery(t,next))}
function passportStats(){const live=trips().filter(t=>!t.archived).sort((a,b)=>String(a.startDate).localeCompare(String(b.startDate)));const countryCounts={},countryDays={},continentCounts={};live.forEach(t=>{const cs=t.countries||inferCountries(t);const days=dateRangeDays(t.startDate,t.endDate);cs.forEach(c=>{countryCounts[c]=(countryCounts[c]||0)+1;countryDays[c]=(countryDays[c]||0)+days;const id=NAME_TO_ID[c.toLowerCase()],cont=COUNTRY_META[id]?.continent||'Other';continentCounts[cont]=(continentCounts[cont]||0)+1})});const entries=Object.entries(countryCounts).sort((a,b)=>b[1]-a[1]);return{live,countryCounts,countryDays,continentCounts,first:live[0],latest:live.at(-1),most:entries[0]?.[0]||'-'}}
function renderPassport(){const s=passportStats();view.innerHTML=`<div class="passport-hero"><div class="card passport-card" id="passportCard"><div class="eyebrow">PASSPORT</div><div class="number">${getCountries().length}/${COUNTRY_TOTAL}</div><div>countries · ${coverage()}% world coverage</div><div class="mini-row" style="margin-top:22px"><span class="pill">${s.live.length} trips</span><span class="pill">${new Set(s.live.flatMap(getCities)).size} cities</span><span class="pill">${s.live.reduce((a,t)=>a+dateRangeDays(t.startDate,t.endDate),0)} days</span></div></div><div class="card card-pad"><div class="stats-grid"><div class="stat"><span class="muted">First trip</span><b style="font-size:17px">${esc(s.first?.title||'-')}</b></div><div class="stat"><span class="muted">Latest</span><b style="font-size:17px">${esc(s.latest?.title||'-')}</b></div><div class="stat"><span class="muted">Most visited</span><b style="font-size:17px">${esc(s.most)}</b></div><div class="stat"><span class="muted">Photos</span><b>${state.travelOS.photoMeta.length}</b></div></div></div></div><div class="section-title"><h2>大陸別</h2><button class="btn" id="sharePassport">Passportを共有</button></div><div class="card card-pad barlist">${['Asia','Europe','Oceania','North America','South America','Africa'].map(c=>{const n=s.continentCounts[c]||0;return `<div class="barrow"><span>${c}</span><div class="progress"><span style="width:${Math.min(100,n*12)}%"></span></div><b>${n}</b></div>`}).join('')}</div><div class="section-title"><h2>国別履歴</h2></div><div class="grid cols3">${Object.entries(s.countryCounts).map(([c,n])=>`<div class="list-item"><h3>${esc(c)}</h3><div class="muted">${n} trips · ${s.countryDays[c]} days</div></div>`).join('')||'<div class="empty">Tripを登録すると集計</div>'}</div><div class="section-title"><h2>Photos</h2><button class="btn" id="addPhotos">＋ 写真を読み込む</button></div><div id="passportPhotos"></div>`;$('#addPhotos').onclick=()=>openPhotoModal();$('#sharePassport').onclick=()=>sharePassportCard();renderPhotosInto($('#passportPhotos'),null,true)}
function renderPhotosInto(el,t,plain=false){const metas=state.travelOS.photoMeta.filter(p=>!t||p.tripId===t.id);el.innerHTML=`${plain?'':`<div class="row wrap"><h2 style="margin:0">Trip Photos</h2><button class="btn" id="addTripPhotos">＋写真</button></div>`}<div class="photo-grid" style="margin-top:12px" id="photoGrid">${metas.map(m=>`<div class="photo" data-photo="${m.id}"><div style="display:grid;place-items:center;height:100%;color:#789">loading…</div><span>${esc(m.city||m.country||'')}</span></div>`).join('')||'<div class="empty" style="grid-column:1/-1">写真を読み込むとGPSと撮影日から整理</div>'}</div>`;if(!plain){const b=$('#addTripPhotos');if(b)b.onclick=()=>openPhotoModal(t)}loadPhotoThumbs(metas)}
async function loadPhotoThumbs(metas){photoUrls.forEach(