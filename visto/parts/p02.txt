COUNTRY_TOTAL)*100).toFixed(1)}
function quickAdd(){if(currentView==='trips')return openTripModal();if(currentView==='bucket')return openBucketModal();if(currentView==='passport')return openPhotoModal();openTripModal()}
function render(){if(leafletMap){leafletMap.remove();leafletMap=null}switch(currentView){case'globe':renderGlobe();break;case'trips':renderTrips();break;case'today':renderToday();break;case'passport':renderPassport();break;case'bucket':renderBucket();break;case'profile':renderProfile();break;}$('#quickAdd').onclick=quickAdd;$('#shareBtn').onclick=()=>shareCurrent();}
function renderGlobe(){const visited=getCountries();view.innerHTML=`<section class="hero"><div class="card globe-card"><div class="globe-top"><div><div class="eyebrow">YOUR WORLD</div><div class="big">${visited.length} countries<br>${coverage()}% explored</div><div class="muted">ドラッグで回転、国をタップ</div></div><div class="pill">● 訪問済み　☆ Bucket</div></div><svg id="globe"></svg><div id="countryPanel" class="country-panel hidden"></div></div><div class="stack"><div class="stats-grid"><div class="stat"><span class="muted">Trips</span><b>${trips().filter(t=>!t.archived).length}</b></div><div class="stat"><span class="muted">Cities</span><b>${new Set(trips().flatMap(getCities)).size}</b></div><div class="stat"><span class="muted">Countries</span><b>${visited.length}</b></div><div class="stat"><span class="muted">Coverage</span><b>${coverage()}%</b></div></div><div class="card card-pad"><div class="section-title" style="margin-top:0"><h2>次の候補</h2><button class="btn" onclick="window._navBucket()">Bucket</button></div>${state.travelOS.bucket.slice(0,4).map(b=>`<div class="row list-item"><div><b>${esc(b.name)}</b><div class="muted">${esc(b.type||'spot')}</div></div><span>${b.done?'✓':'☆'}</span></div>`).join('')||'<div class="empty">Bucket Listを追加するとここに表示</div>'}</div></div></section><div class="section-title"><h2>最近のTrip</h2><button class="btn" onclick="window._navTrips()">すべて見る</button></div><div class="grid cols3">${tripCards(trips().filter(t=>!t.archived).slice(0,6))}</div>`;drawGlobe();}
window._navBucket=()=>navigate('bucket');window._navTrips=()=>navigate('trips');
async function drawGlobe(){const svg=d3.select('#globe');if(svg.empty())return;const node=svg.node(),w=node.clientWidth||700,h=node.clientHeight||520;svg.attr('viewBox',`0 0 ${w} ${h}`);const projection=d3.geoOrthographic().translate([w/2,h/2+35]).scale(Math.min(w,h)*.38).clipAngle(90).rotate([-135,25]);const path=d3.geoPath(projection);svg.append('circle').attr('cx',w/2).attr('cy',h/2+35).attr('r',Math.min(w,h)*.38).attr('fill','#091b31').attr('stroke','#31506f');try{if(!worldData){const [topo,tsv]=await Promise.all([fetch('https://cdn.jsdelivr.net/npm/world-atlas@2/countries-110m.json').then(r=>r.json()),fetch('https://cdn.jsdelivr.net/npm/world-atlas@2/countries-110m.tsv').then(r=>r.text())]);const names=new Map(d3.tsvParse(tsv).map(d=>[d.id,d.name]));worldData=topojson.feature(topo,topo.objects.countries).features;worldData.forEach(f=>f.properties.name=names.get(f.id)||f.id)}const visitedIds=new Set(trips().flatMap(tripCountryIds));const bucketNames=new Set(state.travelOS.bucket.filter(b=>!b.done).map(b=>String(b.country||b.name).toLowerCase()));const g=svg.append('g');const paths=g.selectAll('path').data(worldData).join('path').attr('d',path).attr('fill',d=>visitedIds.has(String(d.id).padStart(3,'0'))?'#4b7dff':bucketNames.has(String(d.properties.name).toLowerCase())?'#f8c95e':'#18304b').attr('stroke','#07111f').attr('stroke-width',.55).on('click',(e,d)=>showCountry(String(d.id).padStart(3,'0'),d.properties.name));const drag=d3.drag().on('drag',e=>{const r=projection.rotate();projection.rotate([r[0]+e.dx*.4,Math.max(-75,Math.min(75,r[1]-e.dy*.4))]);paths.attr('d',path)});svg.call(drag)}catch(err){svg.append('text').attr('x',w/2).attr('y',h/2).attr('text-anchor','middle').attr('fill','#93a8bd').text('地図データを読み込めませんでした') }}
async function showCountry(id,name){let meta=COUNTRY_META[id]||{name,flag:'🌐',currency:'',tz:'',socket:'',continent:''};if(!COUNTRY_META[id]){try{const r=await fetch(`https://restcountries.com/v3.1/name/${encodeURIComponent(name)}?fullText=true&fields=name,flags,currencies,timezones,continents,cca3`);const d=(await r.json())[0];if(d){meta={...meta,flag:d.flags?.emoji||'🌐',currency:Object.keys(d.currencies||{})[0]||'',tz:d.timezones?.[0]||'',continent:d.continents?.[0]||''}}}catch{}}const related=trips().filter(t=>(t.countries||inferCountries(t)).some(c=>c.toLowerCase()===String(meta.name).toLowerCase()));const p=$('#countryPanel');p.innerHTML=`<div class="row"><div><div style="font-size:28px">${meta.flag||'🌐'}</div><h3 style="margin:2px 0">${esc(meta.name)}</h3></div><button class="btn ghost" id="closeCountry">×</button></div><div class="mini-row"><span class="pill">${esc(meta.currency||'通貨不明')}</span><span class="pill">${esc(meta.tz||'TZ不明')}</span>${meta.socket?`<span class="pill">🔌 ${esc(meta.socket)}</span>`:''}</div><p class="muted">Trip ${related.length}件</p>${related.map(t=>`<button class="btn" style="width:100%;margin-top:6px" data-open-trip="${t.id}">${esc(t.title)}</button>`).join('')||'<div class="muted">まだTripなし</div>'}`;p.classList.remove('hidden');$('#closeCountry').onclick=()=>p.classList.add('hidden');p.querySelectorAll('[data-open-trip]').forEach(b=>b.onclick=()=>{selectedTripId=b.dataset.openTrip;navigate('trips')})}
function tripCards(list){return list.map(t=>`<article class="card trip-card" data-trip="${t.id}"><div class="cover">${t.cover?`<img src="${esc(t.cover)}" alt="">`:''}<span class="pill status">${esc(t.status||'Trip')}</span></div><div class="trip-body"><div class="trip-title">${esc(t.title)}</div><div class="muted">${fmtDate(t.startDate)} → ${fmtDate(t.endDate)}</div><div class="mini-row">${(t.countries||inferCountries(t)).map(c=>`<span class="pill">${esc(c)}</span>`).join('')}<span class="pill">${getCities(t).length} cities</span></div></div></article>`).join('')||'<div class="empty">Tripがありません</div>'}
function renderTrips(){const list=trips().filter(t=>!t.archived);if(selectedTripId&&list.some(t=>t.id===selectedTripId))return renderTripDetail(activeTrip());view.innerHTML=`<div class="row wrap"><div><div class="eyebrow">TRIPS</div><div class="big">旅を計画して<br>記録まで残す</div></div><button class="btn primary" id="newTrip">＋ Trip作成</button></div><div class="section-title"><h2>Active / Upcoming</h2><span class="muted">${list.length} trips</span></div><div class="grid cols3" id="tripGrid">${tripCards(list)