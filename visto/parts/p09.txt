
function renderProfile(){const p=state.travelOS.profile||{};view.innerHTML=`<div class="grid cols2"><div class="card card-pad"><div class="row"><div><div class="eyebrow">PROFILE</div><div class="big">${esc(p.name||'Traveler')}</div></div><div style="font-size:54px">🧭</div></div><button class="btn" id="editProfile" style="margin-top:12px">プロフィール編集</button><div class="muted" style="margin-top:8px;font-size:12px">アカウント作成やGoogleログインなしで、そのまま利用</div></div><div class="card card-pad"><h2>Cloud sync</h2><div class="list-item"><div class="row"><div><b>Cloudflare Worker + GitHub</b><div class="muted">複数端末で同じ travel data を共有</div></div><span class="pill" id="cloudStatus">${state.travelOS.settings.cloudSync?'ON':'OFF'}</span></div></div><div class="mini-row"><button class="btn" id="pullCloud">クラウドから取得</button><button class="btn primary" id="pushCloud">クラウドへ保存</button></div></div></div><div class="section-title"><h2>Data</h2></div><div class="grid cols3"><button class="card card-pad btn" id="exportJson">JSONバックアップ</button><button class="card card-pad btn" id="importJson">JSONを復元</button><button class="card card-pad btn" id="installPwa">PWAをインストール</button></div><div class="section-title"><h2>Privacy</h2></div><div class="card card-pad"><div class="list-item"><b>写真は端末内</b><div class="muted">画像本体はIndexedDBに保存。Cloudには写真メタデータだけを保存し、サーバーへ画像を送信しない</div></div><div class="list-item" style="margin-top:8px"><b>共有URL</b><div class="muted">共有時は選択したスナップショットのみURL fragmentへ格納</div></div></div>`;$('#editProfile').onclick=()=>openProfileModal();$('#pullCloud').onclick=pullCloud;$('#pushCloud').onclick=pushCloud;$('#exportJson').onclick=exportJson;$('#importJson').onclick=()=>openImport();$('#installPwa').onclick=()=>window._deferredInstall?.prompt?.()||toast('ブラウザの「ホーム画面に追加」を利用してください')}
function openProfileModal(){const p=state.travelOS.profile||{};modal(`<div class="row"><h2>プロフィール</h2><button class="btn ghost" id="mClose">×</button></div><div class="field"><label>表示名</label><input id="prName" value="${esc(p.name||'Traveler')}"></div><button class="btn primary" id="mSave" style="margin-top:14px;width:100%">保存</button>`);$('#mClose').onclick=closeModal;$('#mSave').onclick=()=>{p.name=$('#prName').value||'Traveler';delete p.googleClientId;delete p.avatar;state.travelOS.profile=p;saveLocal();closeModal();renderProfile()}}
