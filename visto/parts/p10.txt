
Object.assign(CITY_GEO,{Melbourne:[-37.8136,144.9631],Brisbane:[-27.4698,153.0251],Perth:[-31.9523,115.8613],Canberra:[-35.2809,149.13],Kyoto:[35.0116,135.7681],Osaka:[34.6937,135.5023],Hiroshima:[34.3853,132.4553],Munich:[48.1351,11.582],Frankfurt:[50.1109,8.6821],Berlin:[52.52,13.405],Amsterdam:[52.3676,4.9041],Brussels:[50.8503,4.3517],Warsaw:[52.2297,21.0122],HongKong:[22.3193,114.1694],GuatemalaCity:[14.6349,-90.5069],NewYork:[40.7128,-74.006],London:[51.5072,-.1276],Rome:[41.9028,12.4964],Singapore:[1.3521,103.8198]});
function tripPhase(t){const today=new Date().toISOString().slice(0,10);if(t.archived)return'archived';if(t.startDate&&today<t.startDate)return'upcoming';if(t.endDate&&today>t.endDate)return'past';return'current'}
const _renderTripsBase=renderTrips;
renderTrips=function(){const all=trips();if(selectedTripId&&all.some(t=>t.id===selectedTripId))return renderTripDetail(activeTrip());const groups=[['current','旅行中'],['upcoming','予定'],['past','過去'],['archived','Archive']];view.innerHTML=`<div class="row wrap"><div><div class="eyebrow">TRIPS</div><div class="big">旅を計画して<br>記録まで残す</div></div><button class="btn primary" id="newTrip">＋ Trip作成</button></div>${groups.map(([key,label])=>{const list=all.filter(t=>tripPhase(t)===key);return list.length?`<div class="section-title"><h2>${label}</h2><span class="muted">${list.length} trips</span></div><div class="grid cols3">${tripCards(list)}</div>`:''}).join('')||'<div class="empty">Tripがありません</div>'}`;$('#newTrip').onclick=()=>openTripModal();document.querySelectorAll('[data-trip]').forEach(c=>c.onclick=()=>{selectedTripId=c.dataset.trip;renderTrips()})};
const _renderTripTabBase=renderTripTab;
renderTripTab=function(t,tab){const el=$('#tripTab');if(tab==='timeline'){const days=t.days||[];el.innerHTML=`<div class="row wrap"><h2 style="margin:0">日別タイムライン</h2><button class="btn" id="addEvent">＋予定</button></div>${days.map(d=>`<div class="card card-pad" style="margin-top:12px"><div class="row"><div><b>${fmtDate(d.date)} ${esc(d.title||'')}</b><div class="muted">${esc(d.theme||'')}</div></div><span class="pill">${d.items?.length||0} items</span></div><div class="timeline">${(d.items||[]).map(i=>`<div class="event"><div class="dot">${i.flightNumber?'✈️':i.type==='hotel'?'🏨':i.poiId?'📍':i.type==='move'?'🚆':'•'}</div><div class="event-box"><div class="row"><div><strong>${esc(i.time||'')} ${esc(i.title||'')}</strong><div class="event-meta">${esc(i.timezone||'')} ${i.homeTime?`· JST ${esc(i.homeTime)}`:''}${i.flightNumber?` · ${esc(i.flightNumber)} ${esc(i.airline||'')}`:''}</div></div><div class="mini-row"><button class="btn" data-route="${esc(resolveItemQuery(t,i))}">地図</button><button class="btn" data-edit-event="${i.id}">編集</button></div></div>${i.memo?`<div class="muted" style="margin-top:7px">${esc(i.memo)}</div>`:''}${i.bookingRef||i.bookingUrl?`<div class="mini-row">${i.bookingRef?`<span class="pill">予約 ${esc(i.bookingRef)}</span>`:''}${i.bookingUrl?`<a class="btn" href="${esc(i.bookingUrl)}" target="_blank" rel="noopener">予約ページ</a>`:''}</div>`:''}</div></div>`).join('')}</div><div class="photo-grid day-photo-grid" data-photo-day="${d.date}" style="margin-top:10px"></div></div>`).join('')||'<div class="empty">予定を追加してください</div>'}`;$('#addEvent').onclick=()=>openEventModal(t);document.querySelectorAll('[data-route]').forEach(b=>b.onclick=()=>routeFromCurrent(b.dataset.route));document.querySelectorAll('[data-edit-event]').forEach(b=>b.onclick=()=>{const item=(t.days||[]).flatMap(d=>d.items||[]).find(i=>i.id===b.dataset.editEvent);openEventModal(t,item)});renderDayPhotos(t);return}
if(tab==='todo'){el.innerHTML=`<div class="row wrap"><h2 style="margin:0">To-do</h2><button class="btn" id="addTodo">＋To-do</button></div><div class="stack" style="margin-top:12px">${(t.todos||[]).map(x=>`<div class="list-item"><div class="row"><label class="check"><input type="checkbox" data-todo="${x.id}" ${x.status==='done'?'checked':''}><div><h3>${esc(x.title)}</h3><div class="muted">${esc(x.detail||'')} ${x.due?`· ${esc(x.due)}`:''}</div></div></label><button class="btn" data-edit-todo="${x.id}">編集</button></div></div>`).join('')||'<div class="empty">To-doなし</div>'}</div>`;$('#addTodo').onclick=()=>openTodoModal(t);document.querySelectorAll('[data-todo]').forEach(c=>c.onchange=()=>{const x=t.todos.find(y=>y.id===c.dataset.todo);x.status=c.checked?'done':'open';saveLocal()});document.querySelectorAll('[data-edit-todo]').forEach(b=>b.onclick=()=>openTodoModal(t,t.todos.find(x=>x.id===b.dataset.editTodo)));return}
if(tab==='budget'){const exp=t.expenses||[];const paid=exp.filter(x=>x.paid!==false).reduce((a,x)=>a+Number(x.amount||0),0);const unpaid=exp.filter(x=>x.paid===false).reduce((a,x)=>a+Number(x.amount||0),0);const total=paid+unpaid,budget=Number(t.budget||0);el.innerHTML=`<div class="grid cols2"><div class="card card-pad"><div class="muted">Budget</div><div class="big">¥${budget.toLocaleString()}</div><div class="progress"><span style="width:${budget?Math.min(100,total/budget*100):0}%"></span></div><div class="stats-grid" style="margin-top:12px"><div class="stat"><span class="muted">支払済</span><b style="font-size:19px">¥${paid.toLocaleString()}</b></div><div class="stat"><span class="muted">未払い</span><b style="font-size:19px">¥${unpaid.toLocaleString()}</b></div></div></div><div class="card card-pad"><div class="row"><h2 style="margin:0">Expenses</h2><button class="btn" id="addExpense">＋</button></div><div class="stack" style="margin-top:10px">${exp.map(x=>`<button class="row list-item" data-edit-expense="${x.id}" style="width:100%;color:inherit;text-align:left"><div>${esc(x.title)}<div class="muted">${esc(x.category||'')} · ${x.paid===false?'未払い':'支払済'}${x.localAmount?` · ${esc(x.currency||'')} ${Number(x.localAmount).toLocaleString()}`:''}</div></div><b>¥${Number(x.amount||0).toLocaleString()}</b></button>`).join('')||'<div class="empty">支出なし</div>'}</div></div></div>`;$('#addExpense').onclick=()=>openExpenseModal(t);document.querySelectorAll('[data-edit-expense]').forEach(b=>b.onclick=()=>openExpenseModal(t,exp.find(x=>x.id===b.dataset.editExpense)));return}
if(tab==='spots'){_renderTripTabBase(t,tab);setTimeout(()=>{document.querySelectorAll('.list-item').forEach(box=>{const title=box.querySelector('h3')?.textContent?.trim().replace(/^\S+\s/,'');const p=(t.pois||[]).find(x=>x.name===title);if(!p)return;const row=box.querySelector('.mini-row')||box;const btn=document.createElement('button');btn.className='btn';btn.textContent=p.visited?'訪問済 ✓':'訪問済みにする';btn.onclick=()=>{p.visited=!p.visited;p.visitedAt=p.visited?new Date().toISOString():'';if(p.visited)autoCompleteBucket(p.name,p.area);saveLocal();renderTripDetail(t)};row.appendChild(btn)})},0);return}
if(tab==='info'){_renderTripTabBase(t,tab);setTimeout(()=>appendTripInfoTools(t),0);return}
if(tab==='photos'){_renderTripTabBase(t,tab);setTimeout(()=>enhancePhotoView(t),0);return}
_renderTripTabBase(t,tab)};
function routeFromCurrent(destination){const dest=encodeURIComponent(destination);if(!navigator.geolocation)return openMaps(destination);navigator.geolocation.getCurrentPosition(pos=>{const o=`${pos.coords.latitude},${pos.coords.longitude}`;window.open(`https://www.google.com/maps/dir/?api=1&origin=${encodeURIComponent(o)}&destination=${dest}`,'_blank','noopener')},()=>openMaps(destination),{enableHighAccuracy:true,timeout:5000})}
function findEventDay(t,item){return (t.days||[]).find(d=>(d.items||[]).some(i=>i.id===item?.id))}
function openEventModal(t,item=null){const days=t.days||[],day=findEventDay(t,item);modal(`<div class="row"><h2>${item?'予定編集':'予定追加'}</h2><button class="btn ghost" id="mClose">×</button></div><div class="form-grid"><div class="field"><label>日付</label><input type="date" id="eDate" value="${esc(day?.date||days[0]?.date||t.startDate||'')}"></div><div class="field"><label>時刻</label><input type="time" id="eTime" value="${esc(item?.time||'')}"></div><div class="field full"><label>タイトル</label><input id="eTitle" value="${esc(item?.title||'')}"></div><div class="field"><label>種別</label><select id="eType"><option value="spot">Spot</option><option value="flight">Flight</option><option value="hotel">Hotel</option><option value="move">Move</option></select></div><div class="field"><label>JST時刻</label><input type="time" id="eJst" value="${esc(item?.homeTime||'')}"></div><div class="field"><label>便名</label><input id="eFlight" value="${esc(item?.flightNumber||'')}"></div><div class="field"><label>航空会社 / 移動手段</label><input id="eAirline" value="${esc(item?.airline||'')}"></div><div class="field"><label>予約番号</label><input id="eBooking" value="${esc(item?.bookingRef||'')}"></div><div class="field"><label>予約URL</label><input type="url" id="eBookingUrl" value="${esc(item?.bookingUrl||'')}"></div><div class="field full"><label>メモ</label><textarea id="eMemo">${esc(item?.memo||'')}</textarea></div></div><div class="row" style="margin-top:14px">${item?'<button class="btn danger" id="eDelete">削除</button>':'<span></span>'}<button class="btn primary" id="mSave">保存</button></div>`);$('#eType').value=item?.type|| (item?.flightNumber?'flight':'spot');$('#mClose').onclick=closeModal;$('#mSave').onclick=()=>{const date=$('#eDate').value;let target=(t.days||[]).find(d=>d.date===date);if(!target){target={id:uid('day'),date,title:'',theme:'',items:[]};t.days=t.days||[];t.days.push(target);t.days.sort((a,b)=>a.date.localeCompare(b.date))}const x=item||{id:uid('item')};if(item&&day&&day!==target)day.items=day.items.filter(i=>i.id!==item.id);Object.assign(x,{time:$('#eTime').value,title:$('#eTitle').value,homeTime:$('#eJst').value,timezone:'LOCAL',memo:$('#eMemo').value,type:$('#eType').value,flightNumber:$('#eFlight').value,airline:$('#eAirline').value,bookingRef:$('#eBooking').value,bookingUrl:$('#eBookingUrl').value});if(!item||!target.items.some(i=>i.id===x.id))target.items.push(x);target.items.sort((a,b)=>String(a.time).localeCompare(String(b.time)));saveLocal();closeModal();renderTripDetail(t)};if(item)$('#eDelete').onclick=()=>{if(day)day.items=day.items.filter(i=>i.id!==item.id);saveLocal();closeModal();renderTripDetail(t)}}
function openTodoModal(t,item=null){modal(`<div class="row"><h2>${item?'To-do編集':'To-do追加'}</h2><button class="btn ghost" id="mClose">×</button></div><div class="form-grid"><div class="field full"><label>内容</label><input id="tTitle" value="${esc(item?.title||'')}"></div><div class="field full"><label>詳細</label><textarea id="tDetail">${esc(item?.detail||'')}</textarea></div><div class="field"><label>期限</label><input id="tDue" value="${esc(item?.due||'')}"></div><div class="field"><label>担当</label><input id="tOwner" value="${esc(item?.owner||'')}"></div></div><div class="row" style="margin-top:14px">${item?'<button class="btn danger" id="tDelete">削除</button>':'<span></span>'}<button class="btn primary" id="mSave">保存</button></div>`);$('#mClose').onclick=closeModal;$('#mSave').onclick=()=>{const x=item||{id:uid('todo'),status:'open'};Object.assign(x,{title:$('#tTitle').value,detail:$('#tDetail').value,due:$('#tDue').value,owner:$('#tOwner').value});t.todos=t.todos||[];if(!item)t.todos.push(x);saveLocal();closeModal();renderTripDetail(t)};if(item)$('#tDelete').onclick=()=>{t.todos=t.todos.filter(x=>x.id!==item.id);saveLocal();closeModal();renderTripDetail(t)}}
async function fxToJpy(currency,amount){if(!currency||currency==='JPY')return Number(amount||0);try{const r=await fetch(`https://api.frankfurter.app/latest?amount=${encodeURIComponent(amount||1)}&from=${encodeURIComponent(currency)}&to=JPY`);const d=await r.json();return Math.round(d.rates?.JPY||0)}catch{return 0}}
function openExpenseModal(t,item=null){modal(`<div class="row"><h2>${item?'支出編集':'支出追加'}</h2><button class="btn ghost" id="mClose">×</button></div><div class="form-grid"><div class="field full"><label>内容</label><input id="xTitle" value="${esc(item?.title||'')}"></div><div class="field"><label>現地金額</label><input type="number" id="xLocal" value="${Number(item?.localAmount||0)}"></div><div class="field"><label>通貨</label><input id="xCurrency" value="${esc(item?.currency||countryCurrency(t)||'JPY')}"></div><div class="field"><label>JPY</label><input type="number" id="xAmount" value="${Number(item?.amount||0)}"></div><div class="field"><label>カテゴリ</label><select id="xCat"><option>food</option><option>transport</option><option>hotel</option><option>shopping</option><option>activity</option></select></div><div class="field"><label>支払状態</label><select id="xPaid"><option value="true">支払済</option><option value="false">未払い</option></select></div></div><button class="btn" id="xConvert" style="margin-top:10px">現地通貨 → JPY換算</button><div class="row" style="margin-top:14px">${item?'<button class="btn danger" id="xDelete">削除</button>':'<span></span>'}<button class="btn primary" id="mSave">保存</button></div>`);$('#xCat').value=item?.category||'food';$('#xPaid').value=String(item?.paid!==false);$('#mClose').onclick=closeModal;$('#xConvert').onclick=async()=>{$('#xAmount').value=await fxToJpy($('#xCurrency').value.toUpperCase(),Number($('#xLocal').value||0))};$('#mSave').onclick=()=>{const x=item||{id:uid('expense')};Object.assign(x,{title:$('#xTitle').value,localAmount:Number($('#xLocal').value||0),currency:$('#xCurrency').value.toUpperCase(),amount:Number($('#xAmount').value||0),category:$('#xCat').value,paid:$('#xPaid').value==='true'});t.expenses=t.expenses||[];if(!item)t.expenses.push(x);saveLocal();closeModal();renderTripDetail(t)};if(item)$('#xDelete').onclick=()=>{t.expenses=t.expenses.filter(x=>x.id!==item.id);saveLocal();closeModal();renderTripDetail(t)}}
function countryCurrency(t){const id=tripCountryIds(t)[0];return COUNTRY_META[id]?.currency||'JPY'}
function appendTripInfoTools(t){const parent=$('#tripTab');if(!parent)return;const box=document.createElement('div');box.className='card card-pad';box.style.marginTop='12px';const cur=countryCurrency(t);box.innerHTML=`<h2>Travel tools</h2><div class="grid cols2"><div class="list-item"><div class="muted">通貨</div><b>${esc(cur)} / JPY</b><div id="fxLive" class="muted">レート取得中…</div></div><div class="list-item"><div class="muted">経路</div><button class="btn" id="routeDestination">現在地 → ${esc(t.destination||'目的地')}</button></div></div>`;parent.appendChild(box);$('#routeDestination').onclick=()=>routeFromCurrent(t.destination||getCities(t)[0]||'');if(cur!=='JPY')fxToJpy(cur,1).then(v=>{const e=$('#fxLive');if(e)e.textContent=v?`1 ${cur} ≈ ¥${v}`:'レート取得不可'});else $('#fxLive').textContent='1 JPY = ¥1'}
function autoCompleteBucket(name,area=''){const q=(name+' '+area).toLowerCase();state.travelOS.bucket.forEach(b=>{if(!b.done&&[b.name,b.city,b.country].filter(Boolean).some(x=>q.includes(String(x).toLowerCase())))b.done=true})}
