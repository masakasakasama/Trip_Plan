URL.revokeObjectURL);photoUrls=[];for(const m of metas){const blob=await idbGet(m.id);if(!blob)continue;const u=URL.createObjectURL(blob);photoUrls.push(u);const box=document.querySelector(`[data-photo="${m.id}"]`);if(box)box.innerHTML=`<img src="${u}" alt=""><span>${esc(m.city||m.country||'')}</span>`}}
function renderBucket(){const b=state.travelOS.bucket;view.innerHTML=`<div class="row wrap"><div><div class="eyebrow">BUCKET LIST</div><div class="big">次に行きたい場所を<br>同じ地球儀へ</div></div><button class="btn primary" id="newBucket">＋追加</button></div><div class="card card-pad ai-panel" style="margin-top:18px"><div class="row wrap"><div><h2 style="margin:0">✨ Smart import</h2><div class="muted">TikTok / Instagram / YouTube URLから候補名を抽出するローカルデモ</div></div><button class="btn" id="smartImport">URLを解析</button></div></div><div class="section-title"><h2>Wishlist</h2><span class="muted">${b.filter(x=>!x.done).length} pending</span></div><div class="bucket-grid">${b.map(x=>`<div class="bucket ${x.done?'done':''}"><div class="row"><div class="flag">${x.flag||'⭐'}</div><button class="btn ghost" data-bucket-menu="${x.id}">•••</button></div><h3>${esc(x.name)}</h3><div class="muted">${esc(x.country||'')} ${x.city?`· ${esc(x.city)}`:''}</div>${x.memo?`<p>${esc(x.memo)}</p>`:''}<div class="mini-row"><button class="btn" data-bucket-done="${x.id}">${x.done?'未訪問へ':'訪問済み'}</button><button class="btn" data-bucket-trip="${x.id}">Trip作成</button></div></div>`).join('')||'<div class="empty" style="grid-column:1/-1">行きたい国、都市、スポットを保存</div>'}</div><div class="section-title"><h2>AI Planner</h2></div><div class="card card-pad"><div class="row wrap"><div><b>Bucketから旅程を自動生成</b><div class="muted">日程、移動距離、都市を考慮して順番を組む</div></div><button class="btn primary" id="aiPlan">プラン生成</button></div><div id="aiResult" style="margin-top:12px"></div></div>`;$('#newBucket').onclick=()=>openBucketModal();$('#smartImport').onclick=()=>openSmartImport();$('#aiPlan').onclick=()=>generatePlan();document.querySelectorAll('[data-bucket-done]').forEach(x=>x.onclick=()=>{const y=b.find(z=>z.id===x.dataset.bucketDone);y.done=!y.done;saveLocal();renderBucket()});document.querySelectorAll('[data-bucket-trip]').forEach(x=>x.onclick=()=>tripFromBucket(b.find(z=>z.id===x.dataset.bucketTrip)));document.querySelectorAll('[data-bucket-menu]').forEach(x=>x.onclick=()=>{const y=b.find(z=>z.id===x.dataset.bucketMenu);openBucketModal(y)})}
function renderProfile(){const p=state.travelOS.profile||{};view.innerHTML=`<div class="grid cols2"><div class="card card-pad"><div class="row"><div><div class="eyebrow">PROFILE</div><div class="big">${esc(p.name||'Traveler')}</div></div><div style="font-size:54px">${p.avatar?'👤':'🧭'}</div></div><button class="btn" id="editProfile" style="margin-top:12px">プロフィール編集</button><button class="btn" id="googleLogin" style="margin-top:8px;width:100%">G Googleでログイン</button><div class="muted" style="margin-top:8px;font-size:12px">Google Client IDを設定するとGoogle Identity Servicesを利用。未設定時はデモプロフィール</div></div><div class="card card-pad"><h2>Cloud sync</h2><div class="list-item"><div class="row"><div><b>Cloudflare Worker + GitHub</b><div class="muted">複数端末で同じ travel data を共有</div></div><span class="pill" id="cloudStatus">${state.travelOS.settings.cloudSync?'ON':'OFF'}</span></div></div><div class="mini-row"><button class="btn" id="pullCloud">クラウドから取得</button><button class="btn primary" id="pushCloud">クラウドへ保存</button></div></div></div><div class="section-title"><h2>Data</h2></div><div class="grid cols3"><button class="card card-pad btn" id="exportJson">JSONバックアップ</button><button class="card card-pad btn" id="importJson">JSONを復元</button><button class="card card-pad btn" id="installPwa">PWAをインストール</button></div><div class="section-title"><h2>Privacy</h2></div><div class="card card-pad"><div class="list-item"><b>写真は端末内</b><div class="muted">画像本体はIndexedDBに保存。Cloudには写真メタデータだけを保存し、サーバーへ画像を送信しない</div></div><div class="list-item" style="margin-top:8px"><b>共有URL</b><div class="muted">共有時は選択したスナップショットのみURL fragmentへ格納</div></div></div>`;$('#editProfile').onclick=()=>openProfileModal();$('#googleLogin').onclick=()=>googleLogin();$('#pullCloud').onclick=pullCloud;$('#pushCloud').onclick=pushCloud;$('#exportJson').onclick=exportJson;$('#importJson').onclick=()=>openImport();$('#installPwa').onclick=()=>window._deferredInstall?.prompt?.()||toast('ブラウザの「ホーム画面に追加」を利用してください')}
function modal(html){$('#modal').innerHTML=html;$('#modalWrap').classList.add('open');$('#modalWrap').onclick=e=>{if(e.target===$('#modalWrap'))closeModal()}}
function closeModal(){$('#modalWrap').classList.remove('open')}
function openTripModal(t=null){const e=t||{};modal(`<div class="row"><h2>${t?'Trip編集':'Trip作成'}</h2><button class="btn ghost" id="mClose">×</button></div><div class="form-grid"><div class="field full"><label>タイトル</label><input id="fTitle" value="${esc(e.title||'')}"></div><div class="field"><label>開始日</label><input type="date" id="fStart" value="${esc(e.startDate||'')}"></div><div class="field"><label>終了日</label><input type="date" id="fEnd" value="${esc(e.endDate||'')}"></div><div class="field"><label>国（カンマ区切り）</label><input id="fCountries" value="${esc((e.countries||inferCountries(e)).join(', '))}"></div><div class="field"><label>都市（カンマ区切り）</label><input id="fCities" value="${esc(getCities(e).join(', '))}"></div><div class="field"><label>代表都市</label><input id="fDest" value="${esc(e.destination||'')}"></div><div class="field"><label>予算 JPY</label><input type="number" id="fBudget" value="${Number(e.budget||0)}"></div><div class="field full"><label>カバー画像URL</label><input id="fCover" value="${esc(e.cover||'')}"></div><div class="field full"><label>メモ</label><textarea id="fMood">${esc(e.mood||'')}</textarea></div></div><div class="row" style="margin-top:14px"><span></span><button class="btn primary" id="mSave">保存</button></div>`);$('#mClose').onclick=closeModal;$('#mSave').onclick=()=>{const x=t||{id:uid('trip'),todos:[],pois:[],days:[],expenses:[],archived:false,status:'計画中'};Object.assign(x,{title:$('#fTitle').value||'Untitled Trip',startDate:$('#fStart').value,endDate:$('#fEnd').value,countries:$('#fCountries').value.split(',').map(s=>s.trim()).filter(Boolean),cities:$('#fCities').value.split(',').map(s=>s.trim()).filter(Boolean),destination:$('#fDest').value,budget:Number($('#fBudget').value||0),cover:$('#fCover').value,mood:$('#fMood').value});if(!t)state.base.trips.unshift(x);state.base.activeTripId=x.id;selectedTripId=x.id;saveLocal();closeModal();navigate('tri