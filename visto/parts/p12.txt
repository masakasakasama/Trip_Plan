
let lastGeneratedPlan=[];
function bucketGeo(b){const key=b.city||b.name;return CITY_GEO[key]||null}
function smartSummary(text){const clean=String(text||'').replace(/https?:\/\/\S+/g,'').replace(/\s+/g,' ').trim();if(!clean)return'投稿テキストなし。URLと候補地をBucketに保存';const sentences=clean.split(/[。.!?]/).map(s=>s.trim()).filter(Boolean);return sentences.slice(0,2).join('。').slice(0,180)}
async function fetchSocialTitle(url){try{const u=new URL(url);let endpoint='';if(u.hostname.includes('youtube.com')||u.hostname.includes('youtu.be'))endpoint=`https://www.youtube.com/oembed?format=json&url=${encodeURIComponent(url)}`;else if(u.hostname.includes('tiktok.com'))endpoint=`https://www.tiktok.com/oembed?url=${encodeURIComponent(url)}`;if(!endpoint)return'';const r=await fetch(endpoint);if(!r.ok)return'';const d=await r.json();return d.title||''}catch{return''}}
openSmartImport=function(){modal(`<div class="row"><h2>AI Smart Import</h2><button class="btn ghost" id="mClose">×</button></div><div class="field"><label>TikTok / Instagram / YouTube URL</label><input id="siUrl" placeholder="https://..."></div><div class="field" style="margin-top:10px"><label>投稿文 / キャプション（任意）</label><textarea id="siText" placeholder="行きたい場所が分かる文章を貼り付け"></textarea></div><button class="btn primary" id="siGo" style="margin-top:12px;width:100%">解析</button><div id="siResult" style="margin-top:12px"></div>`);$('#mClose').onclick=closeModal;$('#siGo').onclick=async()=>{const u=$('#siUrl').value,txt=$('#siText').value,title=await fetchSocialTitle(u);let slug='';try{const url=new URL(u);slug=decodeURIComponent(url.pathname.split('/').filter(Boolean).at(-1)||'').replace(/[-_]/g,' ').replace(/\d+/g,' ').trim()}catch{}const corpus=`${title} ${txt} ${slug}`;const known=[...Object.values(COUNTRY_META).map(x=>x.name),...Object.keys(CITY_GEO)];const hits=known.filter(k=>corpus.toLowerCase().includes(k.toLowerCase())).slice(0,3);const candidate=hits[0]||title||slug||txt.split(/[,.;。]/)[0]||'New place',summary=smartSummary(`${title}. ${txt}`);$('#siResult').innerHTML=`<div class="list-item"><b>候補: ${esc(candidate)}</b><div class="muted" style="margin-top:5px">${esc(summary)}</div><div class="mini-row">${hits.map(h=>`<span class="pill">${esc(h)}</span>`).join('')}</div><button class="btn" id="siAdd" style="margin-top:8px">Bucketに追加</button></div>`;$('#siAdd').onclick=()=>{const city=Object.keys(CITY_GEO).find(c=>candidate.toLowerCase().includes(c.toLowerCase()))||hits.find(h=>CITY_GEO[h])||'';const country=hits.find(h=>NAME_TO_ID[h.toLowerCase()])||'';state.travelOS.bucket.unshift({id:uid('bucket'),name:candidate,type:city?'city':'spot',country,city,url:u,memo:summary,done:false});saveLocal();closeModal();renderBucket()}}}
function optimizeBucket(items,startCity=''){const pending=[...items],ordered=[];let current=CITY_GEO[startCity]||bucketGeo(pending[0]);if(!current&&pending.length)ordered.push(pending.shift());while(pending.length){if(!current){const x=pending.shift();ordered.push(x);current=bucketGeo(x);continue}let bestI=0,bestD=Infinity;pending.forEach((x,i)=>{const g=bucketGeo(x);if(!g)return;const d=haversineKm(current[0],current[1],g[0],g[1]);if(d<bestD){bestD=d;bestI=i}});const next=pending.splice(bestI,1)[0];ordered.push(next);current=bucketGeo(next)||current}return ordered}
generatePlan=function(){const pending=state.travelOS.bucket.filter(x=>!x.done);const out=$('#aiResult');if(!pending.length){if(out)out.innerHTML='<div class="empty">Bucketを追加してください</div>';return}const t=activeTrip(),ordered=optimizeBucket(pending,t?.destination||''),days=Math.max(1,Math.min(dateRangeDays(t?.startDate,t?.endDate)||3,7));lastGeneratedPlan=Array.from({length:days},()=>[]);let day=0;ordered.forEach((item,i)=>{if(lastGeneratedPlan[day].length>=3&&day<days-1)day++;const prev=lastGeneratedPlan[day].at(-1),a=prev&&bucketGeo(prev),b=bucketGeo(item);const km=a&&b?haversineKm(a[0],a[1],b[0],b[1]):0;item._travelKm=Math.round(km);item._travelMin=km?Math.max(20,Math.round(km/45*60)):0;lastGeneratedPlan[day].push(item)});if(out)out.innerHTML=lastGeneratedPlan.map((items,idx)=>`<div class="list-item"><b>Day ${idx+1}</b>${items.map((x,i)=>`<div style="margin-top:8px">${i?`<span class="muted">↳ 移動 約${x._travelMin}分 / ${x._travelKm}km</span><br>`:''}${i+1}. ${esc(x.name)} <span class="muted">${esc(x.city||x.country||'')}</span></div>`).join('')}</div>`).join('')+`<button class="btn primary" id="applyAiPlan" style="margin-top:10px">このTripの旅程に反映</button><div class="muted" style="margin-top:8px">距離は既知の都市座標から概算。順番は近傍法で最適化</div>`;const apply=$('#applyAiPlan');if(apply)apply.onclick=()=>applyGeneratedPlan(t)}
function applyGeneratedPlan(t){if(!t){toast('反映先Tripがありません');return}const base=t.startDate?new Date(t.startDate+'T12:00:00'):new Date();t.days=t.days||[];lastGeneratedPlan.forEach((items,idx)=>{const date=new Date(base);date.setDate(base.getDate()+idx);const ds=date.toISOString().slice(0,10);let day=t.days.find(d=>d.date===ds);if(!day){day={id:uid('day'),date:ds,title:`Day ${idx+1}`,theme:'AI generated',items:[]};t.days.push(day)}items.forEach((x,i)=>{const h=9+i*3;day.items.push({id:uid('item'),time:`${String(h).padStart(2,'0')}:00`,timezone:'LOCAL',homeTime:'',title:x.name,type:'spot',memo:x.memo||'',aiGenerated:true})})});t.days.sort((a,b)=>a.date.localeCompare(b.date));saveLocal();toast('旅程に反映しました');selectedTripId=t.id;navigate('trips')}
function answerTravelChat(q){const text=String(q||'').toLowerCase(),t=activeTrip();if(!t)return'Tripを作成してください';if(text.includes('明日')||text.includes('tomorrow')){const tomorrow=new Date();tomorrow.setDate(tomorrow.getDate()+1);const ds=tomorrow.toISOString().slice(0,10),d=(t.days||[]).find(x=>x.date===ds);return d?.items?.length?d.items.map(x=>`${x.time||''} ${x.title}`).join('、'):'明日の予定は未登録です'}if(text.includes('予算')||text.includes('budget')){const total=(t.expenses||[]).reduce((a,x)=>a+Number(x.amount||0),0);return`予算 ¥${Number(t.budget||0).toLocaleString()}、記録済み ¥${total.toLocaleString()}、残り ¥${Math.max(0,Number(t.budget||0)-total).toLocaleString()}`}if(text.includes('次')||text.includes('next')){const today=new Date().toISOString().slice(0,10),d=(t.days||[]).find(x=>x.date>=today);return d?.items?.[0]?`${d.items[0].time||''} ${d.items[0].title}`:'次の予定はありません'}if(text.includes('bucket')||text.includes('行きたい')){return state.travelOS.bucket.filter(x=>!x.done).slice(0,5).map(x=>x.name).join('、')||'Bucket Listは空です'}return`${t.title}について、明日の予定、次の予定、予算、Bucket Listを聞けます`}
const _renderBucketBase3=renderBucket;
renderBucket=function(){_renderBucketBase3();const host=view,sec=document.createElement('div');sec.innerHTML=`<div class="section-title"><h2>Travel AI Chat</h2></div><div class="card card-pad ai-panel"><div class="muted">「明日何する？」「予算は？」「次の予定は？」</div><div class="row" style="margin-top:10px"><input id="aiChatInput" style="flex:1;border:1px solid var(--line);background:#071321;color:white;border-radius:10px;padding:10px" placeholder="旅行について質問"><button class="btn primary" id="aiChatSend">送信</button></div><div id="aiChatAnswer" class="list-item" style="margin-top:10px">Tripデータをローカルで参照して回答</div></div>`;host.appendChild(sec);$('#aiChatSend').onclick=()=>{$('#aiChatAnswer').textContent=answerTravelChat($('#aiChatInput').value)};$('#aiChatInput').onkeydown=e=>{if(e.key==='Enter')$('#aiChatSend').click()}}
})();
