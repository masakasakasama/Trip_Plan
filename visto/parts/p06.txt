ps')}}
function openEventModal(t){const days=t.days||[];modal(`<div class="row"><h2>予定追加</h2><button class="btn ghost" id="mClose">×</button></div><div class="form-grid"><div class="field"><label>日付</label><input type="date" id="eDate" value="${days[0]?.date||t.startDate||''}"></div><div class="field"><label>時刻</label><input type="time" id="eTime"></div><div class="field full"><label>タイトル</label><input id="eTitle"></div><div class="field"><label>種別</label><select id="eType"><option value="spot">Spot</option><option value="flight">Flight</option><option value="hotel">Hotel</option><option value="move">Move</option></select></div><div class="field"><label>JST時刻</label><input type="time" id="eJst"></div><div class="field full"><label>メモ / 予約URL</label><textarea id="eMemo"></textarea></div></div><button class="btn primary" id="mSave" style="margin-top:14px;width:100%">追加</button>`);$('#mClose').onclick=closeModal;$('#mSave').onclick=()=>{const date=$('#eDate').value;let day=(t.days||[]).find(d=>d.date===date);if(!day){day={id:uid('day'),date,title:'',theme:'',items:[]};t.days=t.days||[];t.days.push(day);t.days.sort((a,b)=>a.date.localeCompare(b.date))}day.items.push({id:uid('item'),time:$('#eTime').value,title:$('#eTitle').value,homeTime:$('#eJst').value,timezone:'LOCAL',memo:$('#eMemo').value,type:$('#eType').value});saveLocal();closeModal();renderTripDetail(t)}}
function openSpotModal(t){modal(`<div class="row"><h2>Spot追加</h2><button class="btn ghost" id="mClose">×</button></div><div class="form-grid"><div class="field full"><label>名前</label><input id="sName"></div><div class="field"><label>エリア</label><input id="sArea" value="${esc(t.destination||'')}"></div><div class="field"><label>カテゴリ</label><select id="sCat"><option>spot</option><option>food</option><option>hotel</option><option>nature</option></select></div><div class="field full"><label>メモ</label><textarea id="sMemo"></textarea></div></div><button class="btn primary" id="mSave" style="margin-top:14px;width:100%">追加</button>`);$('#mClose').onclick=closeModal;$('#mSave').onclick=()=>{t.pois=t.pois||[];t.pois.push({id:uid('poi'),name:$('#sName').value,area:$('#sArea').value,category:$('#sCat').value,memo:$('#sMemo').value,visited:false});saveLocal();closeModal();renderTripDetail(t)}}
function openTodoModal(t){modal(`<div class="row"><h2>To-do追加</h2><button class="btn ghost" id="mClose">×</button></div><div class="form-grid"><div class="field full"><label>内容</label><input id="tTitle"></div><div class="field full"><label>詳細</label><textarea id="tDetail"></textarea></div><div class="field"><label>期限</label><input id="tDue"></div><div class="field"><label>担当</label><input id="tOwner"></div></div><button class="btn primary" id="mSave" style="margin-top:14px;width:100%">追加</button>`);$('#mClose').onclick=closeModal;$('#mSave').onclick=()=>{t.todos=t.todos||[];t.todos.push({id:uid('todo'),title:$('#tTitle').value,detail:$('#tDetail').value,due:$('#tDue').value,owner:$('#tOwner').value,status:'open'});saveLocal();closeModal();renderTripDetail(t)}}
function openExpenseModal(t){modal(`<div class="row"><h2>支出追加</h2><button class="btn ghost" id="mClose">×</button></div><div class="form-grid"><div class="field full"><label>内容</label><input id="xTitle"></div><div class="field"><label>金額 JPY</label><input type="number" id="xAmount"></div><div class="field"><label>カテゴリ</label><select id="xCat"><option>food</option><option>transport</option><option>hotel</option><option>shopping</option><option>activity</option></select></div></div><button class="btn primary" id="mSave" style="margin-top:14px;width:100%">追加</button>`);$('#mClose').onclick=closeModal;$('#mSave').onclick=()=>{t.expenses=t.expenses||[];t.expenses.push({id:uid('expense'),title:$('#xTitle').value,amount:Number($('#xAmount').value||0),category:$('#xCat').value});saveLocal();closeModal();renderTripDetail(t)}}
function openBucketModal(item=null){const e=item||{};modal(`<div class="row"><h2>${item?'Bucket編集':'Bucket追加'}</h2><button class="btn ghost" id="mClose">×</button></div><div class="form-grid"><div class="field full"><label>名前</label><input id="bName" value="${esc(e.name||'')}"></div><div class="field"><label>種別</label><select id="bType"><option>country</option><option>city</option><option>spot</option></select></div><div class="field"><label>国</label><input id="bCountry" value="${esc(e.country||'')}"></div><div class="field"><label>都市</label><input id="bCity" value="${esc(e.city||'')}"></div><div class="field full"><label>Instagram / TikTok / YouTube URL</label><input id="bUrl" value="${esc(e.url||'')}"></div><div class="field full"><label>メモ</label><textarea id="bMemo">${esc(e.memo||'')}</textarea></div></div><div class="row" style="margin-top:14px">${item?'<button class="btn danger" id="bDelete">削除</button>':'<span></span>'}<button class="btn primary" id="mSave">保存</button></div>`);$('#mClose').onclick=closeModal;$('#mSave').onclick=()=>{const x=item||{id:uid('bucket'),done:false};Object.assign(x,{name:$('#bName').value,type:$('#bType').value,country:$('#bCountry').value,city:$('#bCity').value,url:$('#bUrl').value,memo:$('#bMemo').value});if(!item)state.travelOS.bucket.unshift(x);saveLocal();closeModal();renderBucket()};if(item)$('#bDelete').onclick=()=>{state.travelOS.bucket=state.travelOS.bucket.filter(x=>x.id!==item.id);state.base.travelOS=state.travelOS;saveLocal();closeModal();renderBucket()}}
function openSmartImport(){modal(`<div class="row"><h2>URL Smart Import</h2><button class="btn ghost" id="mClose">×</button></div><div class="field"><label>TikTok / Instagram / YouTube URL</label><input id="siUrl" placeholder="https://..."></div><div class="field" style="margin-top:10px"><label>投稿文（任意、解析精度向上）</label><textarea id="siText" placeholder="Lake Atitlán, Guatemala..."></textarea></div><button class="btn primary" id="siGo" style="margin-top:12px;width:100%">候補抽出</button><div id="siResult" style="margin-top:12px"></div>`);$('#mClose').onclick=closeModal;$('#siGo').onclick=()=>{const u=$('#siUrl').value,txt=$('#siText').value;let candidate='';try{const url=new URL(u);candidate=decodeURIComponent(url.pathname.split('/').filter(Boolean).at(-1)||'').replace(/[-_]/g,' ').replace(/\d+/g,'').trim()}catch{}const known=[...Object.values(COUNTRY_META).map(x=>x.name),...Object.keys(CITY_GEO)];const hit=known.find(k=>(txt+' '+candidate).toLowerCase().includes(k.toLowerCase()));candidate=hit||candidate||txt.split(/[,.
]/)[0]||'New place';$('#siResult').innerHTML=`<div class="list-item"