
const FLAG_FALLBACK={Australia:'🇦🇺',Philippines:'🇵🇭',Japan:'🇯🇵',Germany:'🇩🇪',France:'🇫🇷',Netherlands:'🇳🇱',Belgium:'🇧🇪',Poland:'🇵🇱','Türkiye':'🇹🇷',Turkey:'🇹🇷',Thailand:'🇹🇭',Vietnam:'🇻🇳',China:'🇨🇳','South Korea':'🇰🇷',Korea:'🇰🇷','Hong Kong':'🇭🇰','United States':'🇺🇸',USA:'🇺🇸','United States of America':'🇺🇸',Italy:'🇮🇹'};
function countryFlag(c){const name=String(c||'').trim();const id=NAME_TO_ID[name.toLowerCase()];return COUNTRY_META[id]?.flag||FLAG_FALLBACK[name]||'🌐'}
function flagsForTrip(t){return (t.countries||inferCountries(t)).map(countryFlag).filter(Boolean)}
function tripYearLabel(t){const m=String(t.startDate||tripDateLabel(t)||'').match(/20\d{2}/);return m?m[0]:'TRIP'}
function tripPrimaryLabel(t){return t.destination||getCities(t)[0]||t.title||'Trip'}
function flagStackHtml(flags,klass=''){return `<div class="trip-flag-stack ${klass}">${flags.slice(0,4).map(f=>`<span class="trip-flag">${f}</span>`).join('')}</div>`}
tripCards=function(list){return list.map(t=>{const cs=t.countries||inferCountries(t),flags=flagsForTrip(t),destination=tripPrimaryLabel(t),date=tripDateLabel(t),days=tripDurationDays(t),status=t.status||'Trip',hasPhoto=!!t.cover;return `<article class="trip-card-v2 ${hasPhoto?'has-photo':''}" data-trip="${t.id}">${hasPhoto?`<img class="trip-cover-photo" src="${esc(t.cover)}" alt="">`:''}<div class="trip-card-top">${flagStackHtml(flags)}<span class="trip-year">${esc(tripYearLabel(t))}</span></div><div class="trip-destination">${esc(destination)}</div><div class="trip-country-name">${esc(cs.join(' · '))}${t.title&&t.title!==destination?` · ${esc(t.title)}`:''}</div><div class="trip-date">${esc(date)}</div><div class="trip-meta-v2"><span class="meta-chip">${getCities(t).length} cities</span><span class="meta-chip">${days} days</span><span class="meta-chip ${String(status).includes('旅行中')?'status-now':''}">${esc(status)}</span></div></article>`}).join('')||'<div class="empty">Tripがありません</div>'};
const _renderGlobeFlagBase=renderGlobe;
renderGlobe=function(){_renderGlobeFlagBase();const cs=getCountries();const target=document.querySelector('.globe-top>div');if(target&&cs.length&&!target.querySelector('.flag-rail'))target.insertAdjacentHTML('beforeend',`<div class="flag-rail">${cs.map(c=>`<span class="flag-dot" title="${esc(c)}">${countryFlag(c)}</span>`).join('')}</div>`)};
renderPassport=function(){const s=passportStats(),countries=Object.keys(s.countryCounts),flags=countries.map(countryFlag),cityCount=new Set(s.live.flatMap(getCities)).size,totalDays=s.live.reduce((a,t)=>a+tripDurationDays(t),0);view.innerHTML=`<div class="passport-hero"><div class="card passport-card-v2" id="passportCard"><div class="passport-kicker">TRAVEL PASSPORT</div><div class="passport-number">${countries.length}<span style="font-size:.42em;color:#75879c;font-weight:750"> / ${COUNTRY_TOTAL}</span></div><div class="passport-sub">${coverage()}% of the world · ${s.live.length} trips · ${cityCount} cities</div><div class="passport-flags">${flags.map((f,i)=>`<span title="${esc(countries[i])}">${f}</span>`).join('')}</div></div><div class="card card-pad"><div class="stats-grid"><div class="stat"><span class="muted">First trip</span><b style="font-size:17px">${esc(s.first?.title||'-')}</b></div><div class="stat"><span class="muted">Latest</span><b style="font-size:17px">${esc(s.latest?.title||'-')}</b></div><div class="stat"><span class="muted">Most visited</span><b style="font-size:17px">${countryFlag(s.most)} ${esc(s.most)}</b></div><div class="stat"><span class="muted">Travel days</span><b>${totalDays}</b></div></div></div></div><div class="section-title"><h2>国別履歴</h2><button class="btn" id="sharePassport">Passportを共有</button></div><div class="grid cols3">${Object.entries(s.countryCounts).sort((a,b)=>b[1]-a[1]).map(([c,n])=>`<div class="country-history-card"><div class="country-history-flag">${countryFlag(c)}</div><div><h3>${esc(c)}</h3><div class="country-count">${n} trips · ${s.countryDays[c]} days</div></div></div>`).join('')||'<div class="empty">Tripを登録すると集計</div>'}</div><div class="section-title"><h2>大陸別</h2></div><div class="card card-pad barlist">${['Asia','Europe','Oceania','North America','South America','Africa'].map(c=>{const n=s.continentCounts[c]||0;return `<div class="barrow"><span>${c}</span><div class="progress"><span style="width:${Math.min(100,n*12)}%"></span></div><b>${n}</b></div>`}).join('')}</div><div class="section-title"><h2>Photos</h2><button class="btn" id="addPhotos">＋ 写真を読み込む</button></div><div id="passportPhotos"></div>`;$('#addPhotos').onclick=()=>openPhotoModal();$('#sharePassport').onclick=()=>sharePassportCard();renderPhotosInto($('#passportPhotos'),null,true)};
renderTripDetail=function(t){const tab=(new URLSearchParams(location.hash.slice(1)).get('tab')||'timeline'),tabs=[['timeline','Timeline'],['spots','Spots'],['todo','To-do'],['budget','Budget'],['photos','Photos'],['info','Info']],cs=t.countries||inferCountries(t),flags=flagsForTrip(t);view.innerHTML=`<div class="row wrap"><button class="btn" id="backTrips">← Trips</button><div class="top-actions"><button class="btn" id="editTrip">編集</button><button class="btn" id="archiveTrip">${t.archived?'復元':'Archive'}</button><button class="btn danger" id="deleteTrip">削除</button></div></div><div class="trip-detail-hero-v2"><div class="trip-detail-flags">${flags.slice(0,4).map(f=>`<span class="trip-flag">${f}</span>`).join('')}</div><div class="trip-detail-main"><div class="eyebrow">${esc(cs.join(' · '))}</div><div class="trip-detail-title">${esc(tripPrimaryLabel(t))}</div><div class="trip-detail-sub">${t.title&&t.title!==tripPrimaryLabel(t)?`${esc(t.title)} · `:''}${esc(tripDateLabel(t))}</div></div><div class="trip-detail-side"><div><div class="bigstat">${tripDurationDays(t)}</div><div class="smallstat">DAYS</div></div><div><div class="bigstat">${getCities(t).length}</div><div class="smallstat">CITIES</div></div></div></div><div class="tabs">${tabs.map(([id,l])=>`<button class="tab ${tab===id?'active':''}" data-tab="${id}">${l}</button>`).join('')}</div><div id="tripTab" style="margin-top:14px"></div>`;$('#backTrips').onclick=()=>{selectedTripId='';renderTrips()};$('#editTrip').onclick=()=>openTripModal(t);$('#archiveTrip').onclick=()=>{t.archived=!t.archived;saveLocal();toast('更新しました');selectedTripId='';renderTrips()};$('#deleteTrip').onclick=()=>{if(confirm(`${t.title} を削除しますか？`)){state.base.trips=trips().filter(x=>x.id!==t.id);saveLocal();selectedTripId='';renderTrips()}};document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{location.hash=`tab=${b.dataset.tab}`;renderTripDetail(t)});renderTripTab(t,tab)};
