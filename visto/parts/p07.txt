><b>候補: ${esc(candidate)}</b><div class="muted">外部動画本文そのものはCORS/API制約で直接読めないため、URL slugと入力テキストから抽出</div><button class="btn" id="siAdd" style="margin-top:8px">Bucketに追加</button></div>`;$('#siAdd').onclick=()=>{state.travelOS.bucket.unshift({id:uid('bucket'),name:candidate,type:'spot',country:hit&&NAME_TO_ID[hit.toLowerCase()]?hit:'',city:CITY_GEO[hit]?hit:'',url:u,memo:txt,done:false});saveLocal();closeModal();renderBucket()}}}
function generatePlan(){const pending=state.travelOS.bucket.filter(x=>!x.done);if(!pending.length){$('#aiResult').innerHTML='<div class="empty">Bucketを追加してください</div>';return}const groups={};pending.forEach(x=>{const key=x.city||x.country||'Day trip';(groups[key]=groups[key]||[]).push(x)});let day=1;$('#aiResult').innerHTML=Object.entries(groups).map(([city,items])=>`<div class="list-item"><b>Day ${day++} · ${esc(city)}</b><div class="muted">${items.map(x=>esc(x.name)).join(' → ')}</div></div>`).join('')+`<div class="muted" style="margin-top:8px">ローカルSmart Planner。緯度が登録済みの都市は近い都市単位でまとめる</div>`}
function tripFromBucket(b){const start=new Date();start.setDate(start.getDate()+30);const end=new Date(start);end.setDate(end.getDate()+3);const t={id:uid('trip'),title:`${b.name} Trip`,countries:[b.country||b.name].filter(Boolean),cities:[b.city].filter(Boolean),destination:b.city||b.name,startDate:start.toISOString().slice(0,10),endDate:end.toISOString().slice(0,10),budget:0,status:'計画中',archived:false,todos:[],pois:[{id:uid('poi'),name:b.name,area:b.city||b.country,category:'spot',memo:b.memo||'',visited:false}],days:[],expenses:[]};state.base.trips.unshift(t);b.done=true;saveLocal();selectedTripId=t.id;navigate('trips')}
function openPhotoModal(t=null){modal(`<div class="row"><h2>写真を読み込む</h2><button class="btn ghost" id="mClose">×</button></div><div class="list-item"><b>端末内だけで処理</b><div class="muted">EXIFのGPSと撮影日時を読み取り、画像本体はIndexedDBへ保存。Cloudへ画像アップロードしない</div></div><div class="field" style="margin-top:12px"><label>写真</label><input type="file" id="pFiles" accept="image/*" multiple></div><div class="field" style="margin-top:10px"><label>紐付けTrip</label><select id="pTrip">${trips().map(x=>`<option value="${x.id}" ${t?.id===x.id?'selected':''}>${esc(x.title)}</option>`).join('')}</select></div><button class="btn primary" id="pImport" style="margin-top:12px;width:100%">読み込む</button><div id="pStatus" class="muted" style="margin-top:8px"></div>`);$('#mClose').onclick=closeModal;$('#pImport').onclick=async()=>{const files=[...$('#pFiles').files];const tripId=$('#pTrip').value;if(!files.length)return;let n=0;for(const f of files){let gps=null,date=null;try{const ex=await exifr.parse(f,{gps:true,tiff:true,exif:true});gps=ex?.latitude!=null?[ex.latitude,ex.longitude]:null;date=ex?.DateTimeOriginal||ex?.CreateDate||null}catch{}const meta={id:uid('photo'),tripId,name:f.name,takenAt:date?new Date(date).toISOString():'',lat:gps?.[0]||null,lng:gps?.[1]||null,country:'',city:''};if(gps){const nearest=Object.entries(CITY_GEO).map(([c,g])=>[c,Math.hypot(g[0]-gps[0],g[1]-gps[1])]).sort((a,b)=>a[1]-b[1])[0];if(nearest&&nearest[1]<5)meta.city=nearest[0]}const tr=trips().find(x=>x.id===tripId);meta.country=(tr?.countries||inferCountries(tr||{}))[0]||'';state.travelOS.photoMeta.push(meta);await idbPut(meta.id,f);n++;$('#pStatus').textContent=`${n}/${files.length}`;}saveLocal();closeModal();toast(`${n}枚追加`);render()}}
function openProfileModal(){const p=state.travelOS.profile||{};modal(`<div class="row"><h2>プロフィール</h2><button class="btn ghost" id="mClose">×</button></div><div class="field"><label>表示名</label><input id="prName" value="${esc(p.name||'Traveler')}"></div><div class="field" style="margin-top:10px"><label>Google Client ID（任意）</label><input id="prGoogle" value="${esc(p.googleClientId||'')}"></div><button class="btn primary" id="mSave" style="margin-top:14px;width:100%">保存</button>`);$('#mClose').onclick=closeModal;$('#mSave').onclick=()=>{p.name=$('#prName').value;p.googleClientId=$('#prGoogle').value;state.travelOS.profile=p;saveLocal();closeModal();renderProfile()}}
async function googleLogin(){const p=state.travelOS.profile||{};if(!p.googleClientId){openProfileModal();toast('Google Client IDを設定してください');return}if(!window.google?.accounts?.id){await new Promise((resolve,reject)=>{const s=document.createElement('script');s.src='https://accounts.google.com/gsi/client';s.onload=resolve;s.onerror=reject;document.head.appendChild(s)})}google.accounts.id.initialize({client_id:p.googleClientId,callback:r=>{try{const payload=JSON.parse(atob(r.credential.split('.')[1].replace(/-/g,'+').replace(/_/g,'/')));p.name=payload.name||p.name;p.avatar=payload.picture||'';saveLocal();toast('Googleログイン完了');renderProfile()}catch{toast('ログイン情報を読めませんでした')}}});google.accounts.id.prompt()}
async function pullCloud(){try{const r=await fetch(WORKER,{cache:'no-store'});if(!r.ok)throw new Error(r.status);state.base=await r.json();remoteSha=(r.headers.get('X-Trip-Sha')||r.headers.get('ETag')||'').replaceAll('"','');normalize();state.travelOS.settings.cloudSync=true;saveLocal();$('#syncBadge').textContent='cloud synced';toast('クラウドから取得');renderProfile()}catch(e){toast('クラウド取得失敗 '+e.message)}}
async function pushCloud(){try{if(!remoteSha){const r=await fetch(WORKER,{cache:'no-store'});if(!r.ok)throw new Error('preload '+r.status);remoteSha=(r.headers.get('X-Trip-Sha')||r.headers.get('ETag')||'').replaceAll('"','')}normalize();const r=await fetch(WORKER,{method:'PUT',headers:{'Content-Type':'application/json','If-Match':remoteSha},body:JSON.stringify(state.base)});if(r.status===409){toast('競合あり。先にクラウドから取得してください');return}if(!r.ok)throw new Error(r.status);const j=await r.json();remoteSha=j.sha||remoteSha;state.travelOS.settings.cloudSync=true;saveLocal();$('#syncBadge').textContent='cloud synced';toast('クラウドへ保存')}catch(e){toast('クラウド保存失敗 '+e.message)}}
function exportJson(){const blob=new Blob([JSON.stringify(state.base,null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`trip-os-backup-${new Date().toISOString().slice(0,10)}.json`;a.click();URL.revokeObjectURL(a.href)}
function openImport(){modal(`<div class="row"><h2>JSONを復元</h2><button class="btn ghost" id="mClose">×</button></div><input type="file" id="jsonFile" accept="application/json"><button class="btn primary" id="jsonGo" style="margin-top:12px;width:100%">復元</button>`);$('#mClose').onclick=closeModal;$('#jsonGo').onclick=async()=>{const f=$('#jsonFile').file