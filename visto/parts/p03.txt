}</div>${trips().some(t=>t.archived)?`<div class="section-title"><h2>Archived</h2></div><div class="grid cols3">${tripCards(trips().filter(t=>t.archived))}</div>`:''}`;$('#newTrip').onclick=()=>openTripModal();document.querySelectorAll('[data-trip]').forEach(c=>c.onclick=()=>{selectedTripId=c.dataset.trip;renderTrips()})}
function renderTripDetail(t){const tab=(new URLSearchParams(location.hash.slice(1)).get('tab')||'timeline');const tabs=[['timeline','Timeline'],['spots','Spots'],['todo','To-do'],['budget','Budget'],['photos','Photos'],['info','Info']];view.innerHTML=`<div class="row wrap"><button class="btn" id="backTrips">← Trips</button><div class="top-actions"><button class="btn" id="editTrip">編集</button><button class="btn" id="archiveTrip">${t.archived?'復元':'Archive'}</button><button class="btn danger" id="deleteTrip">削除</button></div></div><div style="margin:18px 0"><div class="eyebrow">${esc((t.countries||inferCountries(t)).join(' · '))}</div><div class="big">${esc(t.title)}</div><div class="muted">${esc(tripDateLabel(t))}　${tripDurationDays(t)}日</div></div><div class="tabs">${tabs.map(([id,l])=>`<button class="tab ${tab===id?'active':''}" data-tab="${id}">${l}</button>`).join('')}</div><div id="tripTab" style="margin-top:14px"></div>`;$('#backTrips').onclick=()=>{selectedTripId='';renderTrips()};$('#editTrip').onclick=()=>openTripModal(t);$('#archiveTrip').onclick=()=>{t.archived=!t.archived;saveLocal();toast('更新しました');selectedTripId='';renderTrips()};$('#deleteTrip').onclick=()=>{if(confirm(`${t.title} を削除しますか？`)){state.base.trips=trips().filter(x=>x.id!==t.id);saveLocal();selectedTripId='';renderTrips()}};document.querySelectorAll('[data-tab]').forEach(b=>b.onclick=()=>{location.hash=`tab=${b.dataset.tab}`;renderTripDetail(t)});renderTripTab(t,tab)}
function renderTripTab(t,tab){const el=$('#tripTab');if(tab==='timeline'){const days=t.days||[];el.innerHTML=`<div class="row wrap"><h2 style="margin:0">日別タイムライン</h2><button class="btn" id="addEvent">＋予定</button></div>${days.map(d=>`<div class="card card-pad" style="margin-top:12px"><div class="row"><div><b>${fmtDate(d.date)} ${esc(d.title||'')}</b><div class="muted">${esc(d.theme||'')}</div></div><span class="pill">${d.items?.length||0} items</span></div><div class="timeline">${(d.items||[]).map(i=>`<div class="event"><div class="dot">${i.flightNumber?'✈️':i.poiId?'📍':'•'}</div><div class="event-box"><div class="row"><div><strong>${esc(i.time||'')} ${esc(i.title||'')}</strong><div class="event-meta">${esc(i.timezone||'')} ${i.homeTime?`· JST ${esc(i.homeTime)}`:''}</div></div>${i.mapsUrl||i.poiId?`<button class="btn" data-route="${esc(resolveItemQuery(t,i))}">地図</button>`:''}</div>${i.memo?`<div class="muted" style="margin-top:7px">${esc(i.memo)}</div>`:''}</div></div>`).join('')}</div></div>`).join('')||'<div class="empty">予定を追加してください</div>'}`;$('#addEvent').onclick=()=>openEventModal(t);document.querySelectorAll('[data-route]').forEach(b=>b.onclick=()=>openMaps(b.dataset.route));}
else if(tab==='spots'){el.innerHTML=`<div class="row wrap"><h2 style="margin:0">Spots</h2><button class="btn" id="addSpot">＋Spot</button></div><div class="card card-pad" style="margin-top:12px"><div id="tripMap" class="map"></div></div><div class="grid cols2" style="margin-top:12px">${(t.pois||[]).map(p=>{const visit=spotVisitLabel(p);return `<div class="list-item"><div class="row"><div><h3>${p.emoji||'📍'} ${esc(p.name)}</h3><div class="muted">${esc(p.area||'')}</div></div><button class="btn" data-spot-map="${esc(p.name)}">Maps</button></div><div class="mini-row"><span class="pill">${esc(p.category||'spot')}</span><span class="pill">${p.visited?'✓ visited':'not yet'}</span>${visit?`<span class="pill">📅 ${esc(visit)}</span>`:''}</div>${p.memo?`<div class="muted" style="margin-top:7px">${esc(p.memo)}</div>`:''}</div>`}).join('')||'<div class="empty">Spotなし</div>'}</div>`;$('#addSpot').onclick=()=>openSpotModal(t);drawTripMap(t);document.querySelectorAll('[data-spot-map]').forEach(b=>b.onclick=()=>openMaps(b.dataset.spotMap));}
else if(tab==='todo'){el.innerHTML=`<div class="row wrap"><h2 style="margin:0">To-do</h2><button class="btn" id="addTodo">＋To-do</button></div><div class="stack" style="margin-top:12px">${(t.todos||[]).map(x=>`<label class="list-item check"><input type="checkbox" data-todo="${x.id}" ${x.status==='done'?'checked':''}><div><h3>${esc(x.title)}</h3><div class="muted">${esc(x.detail||'')} ${x.due?`· ${esc(x.due)}`:''}</div></div></label>`).join('')||'<div class="empty">To-doなし</div>'}</div>`;$('#addTodo').onclick=()=>openTodoModal(t);document.querySelectorAll('[data-todo]').forEach(c=>c.onchange=()=>{const x=t.todos.find(y=>y.id===c.dataset.todo);x.status=c.checked?'done':'open';saveLocal()});}
else if(tab==='budget'){const exp=t.expenses||[];const total=exp.reduce((a,x)=>a+Number(x.amount||0),0);const budget=Number(t.budget||0);el.innerHTML=`<div class="grid cols2"><div class="card card-pad"><div class="muted">Budget</div><div class="big">¥${budget.toLocaleString()}</div><div class="progress"><span style="width:${budget?Math.min(100,total/budget*100):0}%"></span></div><p class="muted">利用 ¥${total.toLocaleString()} / 残り ¥${Math.max(0,budget-total).toLocaleString()}</p></div><div class="card card-pad"><div class="row"><h2 style="margin:0">Expenses</h2><button class="btn" id="addExpense">＋</button></div><div class="stack" style="margin-top:10px">${exp.map(x=>`<div class="row list-item"><div>${esc(x.title)}<div class="muted">${esc(x.category||'')}</div></div><b>¥${Number(x.amount||0).toLocaleString()}</b></div>`).join('')||'<div class="empty">支出なし</div>'}</div></div></div>`;$('#addExpense').onclick=()=>openExpenseModal(t);}
else if(tab==='photos'){renderPhotosInto(el,t);}
else{const countries=(t.countries||inferCountries(t));el.innerHTML=`<div class="grid cols2"><div class="card card-pad"><h2>Trip info</h2><div class="stack"><div class="list-item"><div class="muted">日付</div>${esc(tripDateLabel(t))}</div><div class="list-item"><div class="muted">国</div>${esc(countries.join(', '))}</div><div class="list-item"><div class="muted">都市</div>${esc(getCities(t).join(', '))}</div><div class="list-item"><div class="muted">メモ</div>${esc(t.mood||t.notes||'')}</div>${t.sourceUrl?`<div class="list-item"><div class="muted">Source</div><a href="${esc(t.sourceUrl)}" target="_blank" rel="noopener">${esc(t.source||'Notion')}</a></div>`:''}</div></div><div class="card card-pad"><h2>Live</h2><div id="weatherBox" class="list-item">天気を取得中…</div><div class="list-item" style="margin-top:8px"><div class="muted">現在時刻</div><div id="timeBox"></div></div></div></div>`;loadTripLive(t)} }
function resolveItemQuery(t,i){const p=(t.pois||[]).find(p=>p.id===i.poiId);return p?.name||i.title||t.destination}
function openMaps(q){window.open(`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(q)}`,'_blank','noopener')}
functi